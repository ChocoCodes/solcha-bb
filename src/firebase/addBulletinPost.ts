import { BulletinPost } from '@/utils/types';
import { db } from '@/firebase/firebase';
import { Timestamp, GeoPoint } from 'firebase/firestore';

const addBulletinPost = async (post: BulletinPost): Promise<void> => {

}